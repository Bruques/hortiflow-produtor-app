VITE_API_URL=http://localhost:3001/api

# Spec 25 — public key do Mercado Pago (não é segredo, feita pra ficar exposta no cliente),
# usada só pra tokenizar cartão no cartão mensal recorrente (ver CartaoTokenForm.tsx). Setada
# direto no painel da Vercel em cada projeto (develop e produção), não neste arquivo.
VITE_MP_PUBLIC_KEY=
