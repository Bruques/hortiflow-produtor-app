DATABASE_URL="postgresql://hortiflow_produtor:hortiflow_produtor@localhost:5433/hortiflow_produtor"
PORT=3001
FRONTEND_URL=http://localhost:5173
JWT_SECRET=troque_esse_valor_em_producao

# Spec 18 — assinatura e pagamento
ASAAS_API_URL=https://sandbox.asaas.com/api/v3
ASAAS_API_KEY=
ASAAS_WEBHOOK_TOKEN=
WHATSAPP_CONTATO="(35) 99730-2015"
EMAIL_CONTATO=contato.hortiflow@gmail.com
TRIAL_DIAS=14

# Spec 25 — onboarding e checkout automatizados (Mercado Pago)
MP_API_URL=https://api.mercadopago.com
MP_ACCESS_TOKEN=
MP_WEBHOOK_TOKEN=
# URL pública do backend (Railway), usada só pra montar o notification_url do Mercado Pago
BACKEND_PUBLIC_URL=http://localhost:3001
# Precisa bater com "expo.scheme" em mobile/app.json — usado no deep link de volta pro app
# depois do checkout hospedado do Mercado Pago
MOBILE_APP_SCHEME=hortiflowprodutor
# Só em ambiente de teste (local/staging) — a API de Orders (Pix) exige e-mail @testuser.com
# em modo sandbox. Pegue um "Usuário de teste" no painel do Mercado Pago e monte o e-mail
# como test_user_<numero>@testuser.com. NUNCA setar em produção.
MP_SANDBOX_PAYER_EMAIL=

# Task 24 — importação de lançamentos por foto/PDF/planilha via IA (docs/specs/24)
GEMINI_API_KEY=
